server:
  port: 8080
  context-path: /
spring:
  application:
    name: medical-form
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl